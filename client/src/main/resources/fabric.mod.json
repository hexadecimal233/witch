{
  "schemaVersion": 1,
  "authors": [
    "${author}"
  ],
  "depends": {
    "minecraft": ">=1.19.3"
  },
  "environment": "client",
  "id": "${name}",
  "mixins": [
    "mod.mixins.json"
  ],
  "name": "${name}",
  "version": "${version}"
}
